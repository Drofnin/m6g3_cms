import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here --- held að þetta eigi að vera, sjá mín 27:37*/
  domains: ["www.datacms-assets.com"],
};

export default nextConfig;
